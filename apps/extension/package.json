{
  "name": "@saverlly/extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "build:reviewers": "node scripts/build.js --reviewers",
    "package:reviewers": "npm run build:reviewers && node scripts/package.js",
    "dev": "node scripts/build.js --watch",
    "lint": "eslint \"src/**/*.ts\" --fix",
    "test": "jest",
    "test:browser": "node scripts/verify-browser.js",
    "package": "npm run build && node scripts/package.js"
  },
  "dependencies": {
    "@saverlly/shared-types": "*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/jest": "^30.0.0",
    "archiver": "^7.0.1",
    "esbuild": "^0.24.0",
    "jest": "^30.0.0",
    "jest-environment-jsdom": "^30.0.0",
    "playwright": "^1.62.1",
    "ts-jest": "^29.4.0",
    "typescript": "^5.7.3"
  }
}
