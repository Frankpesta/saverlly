node_modules
# Keep environment variables out of version control
.env
.env.local
.env.test
.env.example
/src/generated/prisma

# User-uploaded files (announcement images, etc.) — not source-controlled
/uploads
